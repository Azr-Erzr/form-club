/* Club-wide defaults. Personal overrides live in Settings (stored on-device). */
window.APP_CONFIG = {
  // Google Sheet ID (the long id in the sheet URL). Blank = bundled data only.
  sheetId: '1-yrjMnyAGWDDZNa4K9NlEACqOn4iTorBwKNSMGEb4-c',
  // Apps Script web app URL (ends in /exec). Blank = logs stay on-device.
  scriptUrl: '',
  // Default display name for a fresh device.
  user: 'Azhar',
};
